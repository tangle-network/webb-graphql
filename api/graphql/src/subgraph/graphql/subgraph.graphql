query  vAnchorList {
  vanchors{
    id
    chainId
    typedChainId
    contractAddress
    token

    volumeComposition {
      id
      token{
        id
        address

      }
      finalValueLocked
      valueLocked
      totalFees
      totalWrappingFees
    }

    numberOfDeposits
    numberOfWithdraws

    minDepositAmount
    maxDepositAmount

    averageDepositAmount

  }
}
