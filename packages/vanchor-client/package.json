{
  "name": "@webb-tools/anchors",
  "main": "./src/index.ts",
  "version": "1.0.1",
  "license": "MIT",
  "scripts": {
    "dev": "graphclient serve-dev",
    "build": "graphclient build",
    "start": "graphclient build && ts-node ./src/index.ts",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@graphprotocol/client-cli": "^3.0.0"
  }
}
