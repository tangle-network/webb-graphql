sources:
  - name: vanchor
    handler:
      graphql:
        endpoint: http://localhost:8000/subgraphs/name/{context.chainName:VAnchorAthenaLocal}

additionalTypeDefs: |
  extend type ShieldedTransaction {
    chainName: String!
  }

additionalResolvers:
  - ./src/resolvers.ts

codegen:
  contextType: 'MeshContext & { chainName: string }'
