{
  "name": "webb-graphql",
  "version": "1.0.0",
  "description": "A collection of Subgraphs for Webb's protocols",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "lint": "nx run-many --all --target=lint",
    "lint:fix": "nx run-many --all --target=lint:fix",
    "format": "prettier --write \"**/*.{ts,js}\" ",
    "format:check": "prettier --check \"**/*.{ts,js}\" ",
    "publish:ci": "lerna run compile && lerna publish --access public patch --yes --message 'chore: release new versions' "
  },
  "devDependencies": {
    "@nx/eslint-plugin": "^16.4.1",
    "eslint-plugin-storybook": "^0.6.12",
    "nx": "^16.4.1",
    "prettier": "^2.8.8",
    "lerna": "^6.6.1"
  }
}
